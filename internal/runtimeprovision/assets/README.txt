Generated UV bundle files are placed here by `go run ./cmd/prepareuv`.

The UV executable and manifest are intentionally ignored by Git. They are
embedded into the Inquira executable during `make build`.
