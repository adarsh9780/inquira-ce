feat(makefile): run frontend tests in aggregate test target

Add test-backend and test-frontend targets to make backend/frontend test execution explicit
Update aggregate test target to run both suites so local/CI checks cover frontend regressions
Add regression assertion in backend/tests/test_makefile_release_workflow.py for the new Makefile test wiring

fix(release): ensure wheel upload can create release and simplify tauri prebuild command

Create draft prerelease when missing before attaching wheel artifact in publish_wheel_to_release job
Replace tauri beforeBuildCommand with npm --prefix frontend invocation to avoid windows command parsing issues
Add regression assertions for release-create fallback and updated tauri prebuild command
