fix(site): correct mkdocs nav paths and relax pages build strictness

Fix mkdocs nav entries to use docs-root relative file names instead of docs/ prefixed paths
Remove strict mode from Pages build command to avoid failing deployment on non-critical warnings
Update docs-site regression tests to match corrected nav mapping and build command
