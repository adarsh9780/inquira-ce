Bump release version to 0.5.0a2 using scripts/maintenance/bump_versions.py and synchronize backend, tauri, frontend, and installer wheel URLs
Fix Tauri release build asset path mismatch by forcing frontend build output to frontend/dist in tauri beforeBuildCommand
Bump project version to 0.5.0a3 (tauri/frontend 0.5.0-alpha.3) via scripts/maintenance/bump_versions.py for a new release tag
Make release workflow platform-aware: build macOS signed only when Apple cert secrets are present, otherwise build unsigned
Use Windows NSIS bundle target in release workflow to avoid MSI prerelease-version restriction on alpha tags
Bump release version to 0.5.0a4 (tauri/frontend 0.5.0-alpha.4) via scripts/maintenance/bump_versions.py
Fix release workflow validation by replacing secrets usage in step-if conditions with job-level env guards
Keep macOS release signing optional (signed only when Apple cert inputs are present; otherwise unsigned build)
Keep Windows release on NSIS bundle target to avoid MSI prerelease version constraint
Bump release version to 0.5.0a5 (tauri/frontend 0.5.0-alpha.5) via scripts/maintenance/bump_versions.py
